import { IActionInterface } from '../actions'
const articles = (state: object[] = [], action: IActionInterface ): object => {
  switch (action.type) {
    default:
      return state
  }
}
export default articles
